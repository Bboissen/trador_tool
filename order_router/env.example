# HTTP server
HOST=0.0.0.0
PORT=8080

# gRPC server
GRPC_PORT=9090

# Database connection (use either DATABASE_URL or the individual parts below)
# Matches order_database/compose.yml defaults (published port 54329)
DATABASE_URL=postgres://postgres:postgres@localhost:54329/orders?sslmode=disable
DB_HOST=localhost
DB_PORT=54329
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=orders
